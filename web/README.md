
  # TutorTime UI Design (Copy)

  This is a code bundle for TutorTime UI Design (Copy). The original project is available at https://www.figma.com/design/9LotkuV0us947qE7G3i64p/TutorTime-UI-Design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  